import React from "react";

const Container = ({ children }) => {
  return (
    <div
      className="max-w-con
    bg-white p-2 m-auto"
    >
      {children}
    </div>
  );
};

export default Container;
